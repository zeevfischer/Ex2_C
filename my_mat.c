#include <stdio.h>
#include "my_mat.h"
#include <math.h>

void build_mat(int *p)
{
    for(int i = 0; i < 3; i++)
    {
        for(int j = 0; j < 3; j++)
        {
            *p=scanf("");
            *p++;
        }
    }
}